package zyBook_Chapter_8;

public class zyBook_8_1_1 {
    //A class describes a set of objects with the same behavior.
    //Definition of class: A programmer-defined data type.

    // class.length()  this is an object
    // class this is class
    // length() this is method inside of the class
    // when we want to use an object, we need call the class then go to the method

    //The set of all methods provided by a class,
    // together with a description of their behavior,
    // is called the public interface of the class.
}
