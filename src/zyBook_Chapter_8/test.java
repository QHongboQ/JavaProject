package zyBook_Chapter_8;

public class test {
    public static void main(String args[])
    {
        // Counter tally = new Counter();
        // tally refers to a Counter object whose value is zero

        // tally.count(); 调用
        // int result = tally.getValue(); 通过class获取值

        /* 定义一个 class 和 instance, 默认值为0
        public class Counter
        {
            private int value;
            public void count()
            {
                value = value + 1;
            }
            public int getValue()
            {
               return value;
            }
        }


        */

    }
}
