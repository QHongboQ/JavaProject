package zyBook_Chapter_8.zyBook_8_2_2;
    // Our first example is a class that models a tally counter,
    // a mechanical device that is used to count people—for example,
    // to find out how many people attend a concert or board a bus.
    // Whenever the operator pushes a button,
    // the counter value advances by one.
    // We model this operation with a count method.
    // A physical counter has a display to show the current value.

    // Construct an object of the Counter class
    // Counter tally = new Counter();
    // Now tally refers to a Counter object whose value is zero.

    // we invoke the count method twice
    // tally.count();
    // tally.count();
    // int result = tally.getValue(); // Sets result to 2

    // Definition of instance variable: A variable defined in a class for which every object of the class has its own value.
    // public class Counter
    // {
    //   private int value;
    //   . . .
    // }
    /**
     This class demonstrates the Counter class.
     */

    /*
    public  class zyBook_8_2_1º

    {

         This class demonstrates the Counter class.

        public static void main(String[] args)
        {
            Counter tally = new Counter();
            tally.count();
            tally.count();
            int result = tally.getValue();
            System.out.println("Value: " + result);
            tally.count();
            tally.count();
            result = tally.getValue();
            System.out.println("Value: " + result);
        }
        }
        */

